# 📤 上传到 GitHub 前必读

## ✅ 已为你准备好的文件

我已经帮你创建了以下文件，让上传过程更顺利：

1. **`.gitignore`** - 自动排除不需要上传的文件
   - ✓ 数据集文件夹（太大）
   - ✓ 模型文件 `.pth`（太大）
   - ✓ Python 缓存
   - ✓ 实验结果（部分保留）

2. **`README_GITHUB.md`** - GitHub 展示用的 README
   - ✓ 项目简介
   - ✓ 核心创新点
   - ✓ 实验结果
   - ✓ 快速开始指南

3. **`LICENSE`** - MIT 开源许可证

4. **`GitHub上传指南.md`** - 详细的上传步骤

5. **`check_git_files.py`** - 检查哪些文件会被上传

6. **`upload_to_github.ps1`** - 一键上传脚本（PowerShell）

---

## 🚀 三种上传方式（选一种即可）

### 方式 1：一键上传脚本（最简单，推荐新手）

```powershell
# 在 PowerShell 中运行
.\upload_to_github.ps1
```

这个脚本会：
- ✓ 检查 Git 是否安装
- ✓ 配置 Git 用户信息
- ✓ 初始化仓库
- ✓ 检查将要上传的文件
- ✓ 提交并推送到 GitHub

---

### 方式 2：命令行手动上传（推荐熟悉 Git 的用户）

```powershell
# 1. 初始化 Git 仓库
git init

# 2. 检查将要上传的文件
python check_git_files.py

# 3. 添加文件
git add .

# 4. 提交
git commit -m "Initial commit: 基于物理先验与深度学习自适应融合的GPR目标分类方法"

# 5. 在 GitHub 上创建仓库后，连接远程仓库
git remote add origin https://github.com/你的用户名/仓库名.git

# 6. 推送
git push -u origin main
```

---

### 方式 3：GitHub Desktop（图形界面，最直观）

1. 下载并安装 [GitHub Desktop](https://desktop.github.com/)
2. 登录你的 GitHub 账号
3. File → Add local repository → 选择项目文件夹
4. 输入提交信息 → Commit
5. Publish repository → 填写仓库信息 → Publish

---

## ⚠️ 上传前检查清单

### 1. 确认 Git 已安装
```powershell
git --version
```
如果没有，下载：https://git-scm.com/download/win

### 2. 检查文件大小
```powershell
python check_git_files.py
```

**重要**：GitHub 单个文件限制 100MB
- 如果有超过 100MB 的文件，会推送失败
- `.gitignore` 已经排除了大部分大文件
- 如果仍有问题，查看下面的"大文件处理"

### 3. 替换 README（可选但推荐）
```powershell
# 备份原 README
mv README.md README_OLD.md

# 使用 GitHub 版本
mv README_GITHUB.md README.md
```

### 4. 修改 LICENSE 中的作者信息
打开 `LICENSE` 文件，将 `[Your Name]` 替换为你的名字

---

## 🔧 常见问题解决

### Q1: 推送时提示文件太大
```powershell
# 查找大文件
git ls-files -s | awk '{if ($4 > 100000000) print $4, $2}'

# 从 Git 中移除
git rm --cached 大文件路径

# 添加到 .gitignore
echo "大文件路径" >> .gitignore

# 重新提交
git add .
git commit --amend
git push
```

### Q2: 推送时要求密码，但密码不对
GitHub 已不支持密码登录，需要使用 **Personal Access Token**：

1. GitHub 网页 → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token
2. 勾选 `repo` 权限
3. 生成并复制 token
4. 推送时用 token 代替密码

### Q3: 如何处理大文件（模型、数据集）

**选项 A：不上传（推荐）**
- 已在 `.gitignore` 中排除
- 在 README 中说明如何获取数据集

**选项 B：使用 Git LFS**
```powershell
# 安装 Git LFS
git lfs install

# 追踪大文件
git lfs track "*.pth"
git lfs track "*.mat"

# 提交 .gitattributes
git add .gitattributes
git commit -m "配置 Git LFS"

# 正常提交大文件
git add .
git commit -m "添加模型文件"
git push
```

**选项 C：使用云存储**
- 上传到 Google Drive / 百度网盘 / OneDrive
- 在 README 中添加下载链接

---

## 📋 推荐的上传内容

### ✅ 应该上传的
- ✓ 源代码（`.py` 文件）
- ✓ 配置文件（`.yaml`, `.json`）
- ✓ 文档（`.md` 文件）
- ✓ 小型示例图片（< 1MB）
- ✓ 许可证和 README

### ❌ 不应该上传的
- ✗ 数据集文件夹（太大）
- ✗ 模型文件 `.pth`（太大，除非使用 Git LFS）
- ✗ Python 缓存 `__pycache__`
- ✗ IDE 配置 `.vscode`, `.idea`
- ✗ 临时文件 `*.tmp`, `*.log`

---

## 🎯 上传后的建议

### 1. 添加仓库描述和标签
在 GitHub 仓库页面：
- About → 添加描述
- Topics → 添加标签：`gpr`, `deep-learning`, `pytorch`, `attention-mechanism`, `radar`

### 2. 创建 Release（可选）
如果有稳定版本：
- Releases → Create a new release
- 上传训练好的模型（如果不太大）

### 3. 添加 GitHub Actions（可选）
自动化测试和部署

### 4. 启用 GitHub Pages（可选）
展示项目文档

---

## 📞 需要帮助？

- 详细步骤：查看 `GitHub上传指南.md`
- Git 命令：查看 `GitHub上传指南.md` 中的"常用命令速查"
- 问题排查：查看 `GitHub上传指南.md` 中的"常见问题"

---

## 🎉 准备好了吗？

选择一种方式开始上传：

**新手推荐**：
```powershell
.\upload_to_github.ps1
```

**熟悉 Git**：
```powershell
git init
git add .
git commit -m "Initial commit"
# 然后在 GitHub 创建仓库并推送
```

**喜欢图形界面**：
- 使用 GitHub Desktop

---

**祝你上传顺利！** 🚀

